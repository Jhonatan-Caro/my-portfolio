import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  subsets: ["latin"],
  variable: "--font-geist-sans",
});

const geistMono = Geist_Mono({
  subsets: ["latin"],
  variable: "--font-geist-mono",
});

export const metadata: Metadata = {
  title: "Jhonatan Caro — Desarrollador, datos e inteligencia artificial",
  description:
    "Portfolio de Jhonatan Caro Suárez, desarrollador full stack enfocado en datos e inteligencia artificial: Python, SQL analítico, PostgreSQL e integración de LLMs con RAG sobre datos estructurados.",
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="es"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="flex min-h-full flex-col">{children}</body>
    </html>
  );
}
